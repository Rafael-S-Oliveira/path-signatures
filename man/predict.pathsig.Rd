% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.pathsig.R
\name{predict.pathsig}
\alias{predict.pathsig}
\title{Fazer Previsões com Modelos de Assinaturas de Caminho}
\usage{
\method{predict}{pathsig}(object, new, s = NULL, Ltype = "list", type = "response", conf = FALSE)
}
\arguments{
\item{object}{Uma lista contendo o modelo treinado e os parâmetros de
normalização (resultado de \code{lasso.pathsig.util} ou similar).}

\item{new}{Uma lista ou matriz contendo os novos caminhos para os quais
se deseja fazer a previsão.}

\item{s}{Valor do parâmetro de penalização lambda a ser usado. Se \code{NULL}
(padrão), utiliza o \code{"lambda.min"} encontrado na validação cruzada do modelo.}

\item{Ltype}{String indicando o tipo de entrada dos novos dados (\code{"list"} por padrão).}

\item{type}{String indicando o tipo de previsão a ser retornada (ex: \code{"response"}, \code{"class"}).}

\item{conf}{Booleano. Se \code{TRUE}, retorna também informações adicionais de
confiança ou erro padrão (caso seja suportado pelo modelo base).}
}
\value{
Se \code{conf = FALSE}, retorna um vetor com os valores preditos.
Se \code{conf = TRUE}, retorna uma lista com as predições (\code{pred})
e as estimativas de erro/confiança (\code{conf}).
}
\description{
Esta função realiza previsões para novos dados utilizando um modelo previamente
ajustado (como os retornados por \code{lasso.pathsig} ou \code{lasso.pathsig.util}).
Ela aplica automaticamente a mesma extração de assinaturas e a mesma normalização
que foram utilizadas durante a fase de treinamento do modelo.
}
