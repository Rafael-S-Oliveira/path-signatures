% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasso.pathsig.util.R
\name{lasso.pathsig.util}
\alias{lasso.pathsig.util}
\title{Ajuste de Modelo Lasso com Assinaturas de Caminho}
\usage{
lasso.pathsig.util(L, y, m, Ltype = "list", family = "gaussian", norm = TRUE)
}
\arguments{
\item{L}{Lista ou matriz contendo os caminhos originais (séries temporais).}

\item{y}{Vetor contendo a variável resposta (target) que o modelo deve prever.}

\item{m}{Número inteiro indicando o nível de truncamento da assinatura.}

\item{Ltype}{String indicando o tipo de entrada (\code{"list"} por padrão).}

\item{family}{String indicando a família do modelo GLM (ex: \code{"gaussian"}, \code{"binomial"}).}

\item{norm}{Booleano. Se \code{TRUE} (padrão), normaliza as assinaturas geradas antes da modelagem.}
}
\value{
Uma lista abrangente contendo o modelo Lasso final ajustado (\code{model}),
  os coeficientes (\code{coef}), o melhor lambda (\code{lambda}), o modelo de
  validação cruzada (\code{cvmodel}), o grau de truncamento (\code{sigdegree}),
  a matriz de assinaturas (\code{sig}), e as médias (\code{meanv}) e desvios
  padrões (\code{sdv}) usados na normalização.
}
\description{
Esta função é o motor principal do pipeline preditivo. Ela recebe uma lista de
caminhos e uma variável resposta, extrai as assinaturas de caminho, aplica
normalização e ajusta um Modelo Linear Generalizado com penalização Lasso
(usando validação cruzada para encontrar o hiperparâmetro ótimo lambda).
}
